# TODO

Дорожка задач на завтра/послезавтра. Актуально после сессии 2026-05-10.

---

## Сейчас (быстрые проверки в UI)

- [ ] Создать студента без лида — должно работать.
- [ ] Создать ещё один филиал — должно работать.
- [ ] Проверить, что у сотрудников `availableBalance = 0` (сброс уже сделан).
- [ ] Попытка выдать зарплату больше, чем `availableBalance` → 400 и тост «Недостаточно баланса».
- [ ] Активировать группу S1 (`/groups/:id` → кнопка «Активировать») — дашборд должен показать уроки.
- [ ] Канбан лидов — перетащить карточку, обновить страницу, порядок сохранился.
- [ ] Должники → выделить → «SMS выбранным» — в Bull Board должна появиться job-ка в `sms`.
- [ ] Рассрочка — создать план на студента, отметить платёж оплаченным.
- [ ] Домашки — создать задание на группу, «Зачесть» сдачу.
- [ ] Админка шаблонов — изменить текст `payment.confirmed`, preview.
- [ ] Экспорт транзакций в Excel — скачать файл.
- [ ] Telegram-привязка — сгенерить код, проверить ссылку `t.me/<bot>?start=<code>`.

---

## Потом (фичи из P1-P2)

- [ ] Student/Parent cabinet — отдельные роуты `/student/*` или `/parent/*` с только своими данными.
- [ ] Click/Payme интеграция: webhook + idempotency + автосписание для рассрочки.
- [ ] 2FA TOTP для CEO/admin.
- [ ] PWA + push notifications (VAPID).
- [ ] Mobile-first журнал посещаемости (swipe-friendly).
- [ ] AI CEO-summary (OpenAI/Claude) по dashboard метрикам.
- [ ] BI-аналитика: когорты, retention, churn, LTV.

---

## Технический долг

- [ ] Локальная Postgres для dev (сейчас Neon us-east-1, ~190 ms/запрос).
- [ ] Redis-кеш на `CoursesService.findAll` и похожие горячие списки.
- [ ] Убрать `TasksService` (старый @Cron) полностью, когда удостоверимся, что BullMQ-scheduler стабилен.
- [ ] Переписать `GroupsModule.providers: [LessonsService]` — должен инжектиться из LessonsModule, а не провайдиться локально.
- [ ] Диф-захват в `AuditInterceptor` (before/after) — сейчас только request body.
- [ ] Обновить `@prisma/client` до 5.19 в соответствии с `prisma@5.19` (warn про mismatch).
- [ ] Фронт bundle 1.28 MB — code-splitting лениво по страницам.
- [ ] Добавить `Reports` другие kind: `students.excel`, `debtors.excel`, PDF.

---

## Вопросы, которые нужно решить

1. **Sentry**: заводим аккаунт или пока без него?
2. **Payments**: Click или Payme первым? Оба нужны в Узбекистане.
3. **Student cabinet**: через Telegram Mini App (быстрее) или web с отдельным логином (универсальнее)?
4. **Локальный Postgres**: надо ли настроить для комфортной разработки?

---
